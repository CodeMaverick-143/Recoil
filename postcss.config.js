export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is actually included in @tailwindcss/postcss usually, but keeping it is fine or removing it.
    },
}
